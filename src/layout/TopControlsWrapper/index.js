import React from 'react'

import './styles.css'

const TopControlsWrapper = ({ children }) => {
  return (
    <div className='top-controls-wrapper' >
      {children}
    </div>
  )
}

export default TopControlsWrapper